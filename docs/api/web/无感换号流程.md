# 无感换号流程 (Device Authorization + Auto Approve)

## 概述

通过已有的 Token，自动完成设备授权，无需用户再次登录。

## 流程图

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   桌面应用       │     │  SSO Portal      │     │  OIDC Endpoint  │
│                 │     │ (portal.sso)     │     │  (oidc)         │
└─────────────────┘     └──────────────────┘     └─────────────────┘
        │                      │                        │
        │  1. 注册 OIDC 客户端  │                        │
        │──────────────────────────────────────────────>│
        │<──────────────────────────────────────────────│
        │     clientId, clientSecret                    │
        │                      │                        │
        │  2. 发起设备授权      │                        │
        │──────────────────────────────────────────────>│
        │<──────────────────────────────────────────────│
        │     deviceCode, userCode, verificationUri     │
        │                      │                        │
        │  3. 验证用户身份 (用已有 Token)                 │
        │─────────────────────>│                        │
        │<─────────────────────│                        │
        │     userIdentifier                            │
        │                      │                        │
        │  4. 获取设备会话令牌   │                        │
        │─────────────────────>│                        │
        │<─────────────────────│                        │
        │     deviceSessionToken (JWT)                  │
        │                      │                        │
        │  5. 接收用户代码      │                        │
        │──────────────────────────────────────────────>│
        │<──────────────────────────────────────────────│
        │     authorizationResumptionContext            │
        │                      │                        │
        │  6. 批准设备授权      │                        │
        │──────────────────────────────────────────────>│
        │<──────────────────────────────────────────────│
        │     授权成功                                   │
        │                      │                        │
        │  7. 轮询获取 Token    │                        │
        │──────────────────────────────────────────────>│
        │<──────────────────────────────────────────────│
        │     accessToken, refreshToken                 │
        │                      │                        │
        │  8. 保存到本地        │                        │
        │  ~/.aws/sso/cache/kiro-auth-token.json        │
```

## 详细步骤

### Step 1: 注册 OIDC 客户端

**端点**: `POST https://oidc.{region}.amazonaws.com/client/register`

**请求**:
```json
{
  "clientName": "Kiro-BuilderId",
  "clientType": "public",
  "scopes": [
    "codewhisperer:completions",
    "codewhisperer:analysis",
    "codewhisperer:conversations",
    "codewhisperer:transformations",
    "codewhisperer:taskassist"
  ],
  "grantTypes": ["authorization_code", "refresh_token"],
  "redirectUris": ["http://127.0.0.1/oauth/callback"],
  "issuerUrl": "https://identitycenter.amazonaws.com/ssoins-xxx"
}
```

**响应**:
```json
{
  "clientId": "xxx",
  "clientSecret": "xxx",
  "clientIdIssuedAt": 1764520000,
  "clientSecretExpiresAt": 1767112000
}
```

---

### Step 2: 发起设备授权

**端点**: `POST https://oidc.{region}.amazonaws.com/device_authorization`

**请求**:
```json
{
  "clientId": "xxx",
  "clientSecret": "xxx",
  "startUrl": "https://view.awsapps.com/start"
}
```

**响应**:
```json
{
  "deviceCode": "xxx",
  "userCode": "XXXX-XXXX",
  "verificationUri": "https://device.sso.{region}.amazonaws.com/",
  "verificationUriComplete": "https://device.sso.{region}.amazonaws.com/?user_code=XXXX-XXXX",
  "expiresIn": 600,
  "interval": 1
}
```

---

### Step 3: 验证用户身份 (用已有 Token)

**端点**: `GET https://portal.sso.{region}.amazonaws.com/token/whoAmI`

**请求头**:
```http
Authorization: Bearer <已有的 JWE Token>
Accept: application/json
```

**响应**:
```json
{
  "userIdentifier": "base64...",
  "accountId": "432677196278",
  "directoryId": "d-9067642ac7",
  "authenticationType": "NATIVE",
  "identityStoreUserId": "44a85468-40e1-708e-e018-01ce81f12cf5",
  "tokenType": "NATIVE",
  "createDate": 1764863388000,
  "expireDate": 1767455385000
}
```

---

### Step 4: 获取设备会话令牌

**端点**: `POST https://portal.sso.{region}.amazonaws.com/session/device`

**请求头**:
```http
Authorization: Bearer <已有的 JWE Token>
Content-Type: application/json
```

**请求体**: `{}`

**响应**:
```json
{
  "token": "<设备会话 JWT>"
}
```

---

### Step 5: 接收用户代码

**端点**: `POST https://oidc.{region}.amazonaws.com/device_authorization/accept_user_code`

**请求**:
```json
{
  "userCode": "XXXX-XXXX"
}
```

**响应**:
```json
{
  "authorizationResumptionContext": "<加密 blob>"
}
```

---

### Step 6: 批准设备授权

**端点**: `POST https://oidc.{region}.amazonaws.com/device_authorization/associate_token`

**请求**:
```json
{
  "authorizationResumptionContext": "<加密 blob>",
  "userSessionId": "<设备会话 JWT>"
}
```

**响应**:
```json
{
  "location": null
}
```

---

### Step 7: 轮询获取 Token

**端点**: `POST https://oidc.{region}.amazonaws.com/token`

**请求**:
```json
{
  "clientId": "xxx",
  "clientSecret": "xxx",
  "grantType": "urn:ietf:params:oauth:grant-type:device_code",
  "deviceCode": "xxx"
}
```

**轮询中响应**:
```json
{
  "error": "authorization_pending",
  "error_description": "User has not yet completed authorization"
}
```

**成功响应**:
```json
{
  "access_token": "aoaAAAAA...",
  "refresh_token": "aorAAAAA...",
  "token_type": "Bearer",
  "expires_in": 28800
}
```

---

### Step 8: 保存到本地

**文件**: `~/.aws/sso/cache/kiro-auth-token.json`

```json
{
  "accessToken": "aoaAAAAA...",
  "refreshToken": "aorAAAAA...",
  "expiresAt": "2025-12-05T07:54:17.089Z",
  "clientIdHash": "e909a0580879b06ece1202964fbe9dda95ea4ce3",
  "authMethod": "IdC",
  "provider": "BuilderId",
  "region": "us-east-1"
}
```

---

## 关键点

1. **无感换号的核心**: 用已有的 Token 调用 `whoAmI` 和 `session/device`，获取设备会话 JWT
2. **自动批准**: 用设备会话 JWT 调用 `accept_user_code` 和 `associate_token`，自动批准授权
3. **无需浏览器**: 整个流程不需要打开浏览器，用户无感知
4. **轮询机制**: 后端需要轮询 `/token` 端点，等待授权完成

---

## 与普通 Device Flow 的区别

| 项目 | 普通 Device Flow | 无感换号 |
|------|-----------------|---------|
| 用户操作 | 打开浏览器，输入 userCode | 无需操作 |
| 需要已有 Token | ❌ | ✅ |
| 自动批准 | ❌ | ✅ |
| 适用场景 | 首次登录 | 换号/刷新 |

---

## 端点汇总

| 端点 | 方法 | 用途 |
|------|------|------|
| `oidc.{region}.amazonaws.com/client/register` | POST | 注册客户端 |
| `oidc.{region}.amazonaws.com/device_authorization` | POST | 发起设备授权 |
| `portal.sso.{region}.amazonaws.com/token/whoAmI` | GET | 验证身份 |
| `portal.sso.{region}.amazonaws.com/session/device` | POST | 获取设备会话 |
| `oidc.{region}.amazonaws.com/device_authorization/accept_user_code` | POST | 接收用户代码 |
| `oidc.{region}.amazonaws.com/device_authorization/associate_token` | POST | 批准授权 |
| `oidc.{region}.amazonaws.com/token` | POST | 获取 Token |
