# 功能逻辑对比

## 1. 获取用量数据

### chaogei
```
API: https://app.kiro.dev/service/KiroWebPortalService/operation/GetUserUsageAndLimits
编码: CBOR
请求头: cookie: Idp=${idp}; AccessToken=${accessToken}

返回数据解析:
- usageBreakdownList[0] 找 resourceType === 'CREDIT'
- 基础额度: usageLimit / currentUsage
- 试用额度: freeTrialInfo.usageLimit / currentUsage (freeTrialStatus === 'ACTIVE')
- 奖励额度: bonuses[] 数组，每个有 usageLimit / currentUsage / expiresAt
- 总额度 = 基础 + 试用 + 所有奖励
```

### 你的项目
```
API: 
- BuilderId: https://codewhisperer.us-east-1.amazonaws.com/ (x-amz-target: GetUsageLimits)
- Social: https://prod.us-east-1.codewhisperer.desktop.kiro.dev/GetUsageLimits
编码: JSON

返回数据: 直接存储原始 JSON 到 usage_data 字段，前端自行解析
```

**差异**: chaogei 后端解析好结构化数据，你的项目前端解析原始 JSON

---

## 2. Token 刷新

### 两者相同
```
IdC (BuilderId):
  POST https://oidc.{region}.amazonaws.com/token
  body: { clientId, clientSecret, refreshToken, grantType: 'refresh_token' }

Social (Google/Github):
  POST https://prod.us-east-1.auth.desktop.kiro.dev/refreshToken
  body: { refreshToken }
```

---

## 3. 账号切换

### chaogei
```
1. 写入 ~/.aws/sso/cache/kiro-auth-token.json
   - Social: { accessToken, refreshToken, profileArn, authMethod: 'social', provider }
   - IdC: { accessToken, refreshToken, clientIdHash, authMethod: 'IdC', region }

2. IdC 额外写入 ~/.aws/sso/cache/{clientIdHash}.json
   - { clientId, clientSecret, expiresAt }

不涉及 IDE 进程操作
```

### 你的项目
```
1. 可选: 关闭 Kiro IDE (如果需要重置机器ID)
2. 可选: 重置 Kiro 机器ID (storage.json + state.vscdb)
3. 写入 kiro-auth-token.json (同上)
4. IdC 额外写入 {clientIdHash}.json (同上)
5. 可选: 重启 Kiro IDE
```

**差异**: 你的项目集成了机器ID重置和IDE进程管理

---

## 4. 机器码管理

### chaogei - 系统级
```
Windows: reg add "HKLM\SOFTWARE\Microsoft\Cryptography" /v MachineGuid
macOS: 写入应用层覆盖文件 (无效，Kiro 不读这个)
Linux: 写入 /etc/machine-id
```

### 你的项目 - 双层
```
系统级 (machine_guid_cmd.rs):
  Windows: 修改注册表 MachineGuid
  macOS: 明确返回错误 "无法重置"

Kiro IDE 级 (kiro.rs):
  修改 storage.json: telemetry.machineId, sqmId, devDeviceId
  修改 state.vscdb: storage.serviceMachineId
```

**差异**: 你区分了系统机器码和 Kiro 遥测ID，chaogei 只改系统机器码

---

## 5. 自动刷新

### chaogei
```
定时器: setInterval(checkAndRefreshExpiringTokens, interval * 60 * 1000)

逻辑:
1. 遍历所有账号
2. 跳过已封禁的 (lastError 包含 UnauthorizedException)
3. Token 过期前 5 分钟刷新
4. 刷新后同时调用 checkAccountStatus 更新用量
5. 如果开启自动换号，还会检查余额
```

### 你的项目
```
无自动刷新定时器
手动调用 sync_account 刷新单个账号
```

---

## 6. 自动换号 (chaogei 独有)

```
定时器: setInterval(checkAndAutoSwitch, autoSwitchInterval * 60 * 1000)

逻辑:
1. 获取当前激活账号
2. 刷新其状态获取最新余额
3. 计算剩余 = limit - current
4. 如果剩余 <= 阈值:
   - 遍历所有账号找可用的
   - 排除当前账号
   - 排除已封禁的
   - 排除余额也不足的
   - 找到后调用 switchAccount 切换
```

---

## 7. 账户机器码绑定 (chaogei 独有)

```
数据结构:
  accountMachineIds: Record<accountId, machineId>

切换账号时:
1. 如果开启 autoSwitchOnAccountChange:
   - 如果开启 bindMachineIdToAccount:
     - 查找该账号绑定的机器码
     - 没有则生成一个并绑定
     - 如果 useBindedMachineId 则使用绑定的
     - 否则随机生成新的
   - 否则每次随机生成
2. 调用系统 API 修改机器码
```

---

## 8. SSO Token 导入 (chaogei 独有)

```
输入: x-amz-sso_authn Cookie 值 (Bearer Token)

流程:
1. 注册 OIDC 客户端 -> 获取 clientId, clientSecret
2. 发起设备授权 -> 获取 deviceCode, userCode
3. 用 Bearer Token 调用 whoAmI 验证
4. 获取设备会话令牌
5. 接受用户代码
6. 批准授权
7. 轮询获取 Token
8. 调用 GetUserUsageAndLimits 获取用户信息
9. 添加账号

支持批量: 每行一个 Token，并发导入
```

---

## 9. 你的项目独有

### MCP 管理
```
读取: ~/.kiro/settings/mcp.json
操作: 添加/删除/启用/禁用 MCP 服务器
```

### Powers 管理
```
读取: ~/.kiro/powers/ 目录下的 Powers
```

### IDE 进程管理
```
检测: 查找 Kiro.exe / Kiro 进程
关闭: taskkill / kill
启动: 打开 Kiro 可执行文件
```

### Web OAuth 登录
```
通过 Web 页面登录获取 RefreshToken Cookie
解析 Cookie 添加账号
```

---

## 10. 封禁检测

### chaogei
```
API 返回 401 且 error 包含:
- UnauthorizedException
- AccountSuspendedException

标记账号状态为 error，UI 显示 "已封禁"
自动刷新/换号时跳过这些账号
```

### 你的项目
```
API 返回错误以 "BANNED:" 开头
标记账号 status = "已封禁"
```

逻辑相同，只是错误格式不同
